# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.3

# LLM
openai>=1.30.0
litellm>=1.40.0

# Vector DB
# Pinned to match server version 1.7.4 exactly — qdrant-client 1.18.0 vs
# server 1.7.4 caused a major version mismatch warning and is the likely
# second contributor to silent vector corruption over gRPC.
qdrant-client==1.7.3

# Embeddings + reranking
# Pinned exact — unbounded >= caused a version drift where encode() returned
# 0-dim vectors for BAAI/bge-large-en-v1.5 on newer releases. Do not loosen
# this without re-running the dimension probe in EmbeddingService on startup.
sentence-transformers==3.0.1
torch==2.3.1
transformers==4.42.4
huggingface-hub==0.24.5

# BM25 / FTS
elasticsearch[async]>=8.13.0   # optional: set ES_ENABLED=true to activate

# NLP
spacy>=3.7.0
# python -m spacy download en_core_web_sm

# PDF parsing
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
pdf2image>=1.17.0
pytesseract>=0.3.10
python-docx>=1.1.0

# Redis cache
redis[hiredis]>=5.0.0

# Distributed ingestion (optional)
celery[redis]>=5.4.0

# Rate limiting
slowapi>=0.1.9

# Utils
python-multipart>=0.0.9
httpx>=0.27.0
aiofiles>=23.2.1
tenacity>=8.3.0

# Celery monitoring
flower>=2.0.1

# Missing utility deps
python-dateutil>=2.9.0
ujson>=5.9.0
